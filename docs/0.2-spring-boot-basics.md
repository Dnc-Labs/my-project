# 0.2 - Spring Boot Basics

> Buổi học ngày 2026-03-30

---

## 1. Cấu trúc project Maven

```
api/src/
├── main/           ← Code chính của ứng dụng
│   ├── java/       ← Viết code Java ở đây
│   │   └── com/ecommerce/api/
│   │       ├── ApiApplication.java
│   │       ├── controllers/
│   │       ├── services/
│   │       └── dto/
│   └── resources/  ← File cấu hình (application.yaml, templates...)
│
└── test/           ← Code test (JUnit)
    └── java/
        └── com/ecommerce/api/
```

| Thư mục | Mục đích | Đóng gói vào .jar? |
|---|---|---|
| `src/main/java` | Code ứng dụng chạy thật | Có |
| `src/main/resources` | File cấu hình | Có |
| `src/test/java` | Code test (JUnit, Mockito) | Không |

> So sánh Node.js: `src/` ↔ `src/main/java/`, `test/` hoặc `__tests__/` ↔ `src/test/java/`

**Lưu ý**: `src/main/java/` chỉ là đường dẫn vật lý của Maven. Package Java bắt đầu **sau** `java/`:

```
Đường dẫn:  src/main/java/com/ecommerce/api/services/MyService.java
Package:    com.ecommerce.api.services   ✅
KHÔNG phải: src.main.java.com.ecommerce.api.services   ❌
```

---

## 2. Layered Architecture

```
Client (HTTP Request)
        ↓
Controller  — Nhận request, gọi Service, trả response
        ↓
Service     — Business logic
        ↓
Repository  — Truy cập database
        ↓
Entity      — Đại diện bảng trong database
```

Ngoài ra còn có **DTO** (Data Transfer Object) — object dùng để truyền dữ liệu giữa các layer hoặc trả về cho client, tách biệt khỏi Entity.

---

## 3. REST API trong Spring Boot

```java
@RestController                    // Đánh dấu class xử lý HTTP, trả JSON
@RequestMapping("/greeting")       // Base path
public class GreetingController {

    private final GreetingService greetingService;

    // Constructor Injection
    GreetingController(GreetingService greetingService) {
        this.greetingService = greetingService;
    }

    @GetMapping                    // GET /greeting
    ResponseEntity<String> getGreeting(@RequestParam(required = false) String name) {
        String result = this.greetingService.handleGetGreeting(name);
        return ResponseEntity.ok(result);
    }
}
```

### Các annotation HTTP mapping

| Annotation | HTTP Method |
|---|---|
| `@GetMapping` | GET |
| `@PostMapping` | POST |
| `@PutMapping` | PUT |
| `@DeleteMapping` | DELETE |
| `@PatchMapping` | PATCH |

### `@GetMapping` vs `@GetMapping("/")`

- `@RequestMapping("/greeting")` + `@GetMapping("/")` → chỉ match `/greeting/` (có slash cuối)
- `@RequestMapping("/greeting")` + `@GetMapping` → match cả `/greeting` lẫn `/greeting/`

### `@RequestParam` — nhận query parameter

```java
// GET /greeting?name=Avada
@GetMapping
ResponseEntity<String> getGreeting(@RequestParam(required = false) String name) {
    // required = false → name có thể null
}
```

### `ResponseEntity` — kiểm soát HTTP response

```java
// Cách đầy đủ
return new ResponseEntity<>(body, HttpStatus.OK);

// Cách gọn
return ResponseEntity.ok(body);
```

### Trả về JSON — Spring Boot tự động convert

Bên Node.js dùng `res.json({ name, version })` — truyền object literal. Bên Java không có object literal, nên tạo một **DTO class**:

```java
// Tạo DTO
public class AppInfoResponse {
    private final String name;
    private final String version;
    // constructor + getter
}

// Trả về trong Controller — Spring tự chuyển thành JSON nhờ Jackson
return ResponseEntity.ok(new AppInfoResponse("my-app", "1.0"));
// → {"name": "my-app", "version": "1.0"}
```

**Tại sao dùng DTO class thay vì `Map<String, String>`?**
- **Type-safe** — compiler kiểm tra, không sợ typo key
- **Rõ ràng** — nhìn class biết ngay API trả về gì
- **Dễ mở rộng** — thêm validation, annotation sau này dễ dàng

`Map` nhanh cho prototype, nhưng dự án thật luôn dùng DTO.

---

## 4. `application.yml` — File cấu hình

### Config chuẩn của Spring vs config tự định nghĩa

```yaml
# Config chuẩn Spring — phải đúng key, Spring tự đọc
server:
  port: 8081
spring:
  application:
    name: my-app
  datasource:
    url: jdbc:mysql://localhost:3306/mydb

# Config tự định nghĩa — đặt tên tùy ý, tự đọc bằng @Value
app:
  name: my-app
  version: "1.0"
  description: "This is learning app"
```

### Tra cứu config chuẩn của Spring

1. **Official docs**: https://docs.spring.io/spring-boot/appendix/application-properties/
2. **IDE auto-complete**: IntelliJ gợi ý khi gõ trong `application.yml`
3. **Đọc class `*Properties`**: mỗi starter có class chứa các config (ví dụ `DataSourceProperties`)

### `.properties` vs `.yml`

```properties
# application.properties — phẳng, lặp prefix
server.port=8081
spring.datasource.url=jdbc:mysql://localhost:3306/mydb
```

```yaml
# application.yml — phân cấp, dễ đọc
server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
```

Cả 2 hoạt động như nhau. Dự án thực tế phần lớn dùng `.yml`.

### Profiles — cấu hình theo môi trường

```
src/main/resources/
├── application.yml           # Config chung
├── application-dev.yml       # Môi trường dev
└── application-prod.yml      # Môi trường production
```

Kích hoạt profile:

```yaml
# application.yml
spring:
  profiles:
    active: dev    # hoặc qua command line: --spring.profiles.active=prod
```

### Quản lý secret trên production

**Không bao giờ đẩy password, secret key lên Git!**

**Cách 1: Environment Variables (phổ biến nhất)**

```yaml
# application-prod.yml — commit được vì không chứa secret thật
spring:
  datasource:
    url: ${DB_URL}               # Đọc từ biến môi trường
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
```

```bash
# Trên server production
export DB_URL=jdbc:mysql://prod-server:3306/ecommerce
export DB_USERNAME=prod_user
export DB_PASSWORD=super_secret_123
```

Spring Boot **tự động** thay `${DB_URL}` bằng giá trị biến môi trường.

**Cách 2: File config riêng + `.gitignore`**

```
src/main/resources/
├── application.yml              # Config chung → commit
├── application-dev.yml          # Dev → commit (không có secret)
├── application-prod.yml         # ❌ KHÔNG commit, thêm vào .gitignore
```

Trên server prod, đặt file `application-prod.yml` trực tiếp trên máy hoặc mount vào container.

**Cách 3: Kết hợp cả 2 (thực tế hay dùng nhất)**

```yaml
# application-dev.yml — commit, dev dùng local
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_dev
    username: root
    password: root123
  jpa:
    show-sql: true

# application-prod.yml — commit, nhưng secret lấy từ env
spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: false
```

> So sánh Node.js: `.env` + `dotenv` ↔ `application.yml` + env var. `process.env.DB_URL` ↔ `${DB_URL}` trong yml.

> **Tư duy senior**: Ở quy mô lớn hơn, các công ty dùng **Vault** (HashiCorp), **AWS Secrets Manager**, hoặc **Spring Cloud Config Server** để quản lý secret tập trung. Cho dự án hiện tại, Environment Variables là đủ và chuẩn.

### Đọc config trong code

```java
@Service
public class AppInfoService {

    private final String appName;

    public AppInfoService(@Value("${app.name}") String appName) {
        this.appName = appName;
    }
}
```

---

## 5. Bean Lifecycle — Vòng đời của Bean

```
1. Instantiation — Spring tạo object (gọi constructor)
        ↓
2. Dependency Injection — Tiêm dependency vào
        ↓
3. @PostConstruct — Chạy method khởi tạo sau khi inject xong
        ↓
4. Bean sẵn sàng sử dụng ✅
        ↓
   ... (ứng dụng chạy) ...
        ↓
5. @PreDestroy — Chạy method dọn dẹp trước khi Bean bị hủy
        ↓
6. Bean bị hủy (khi ứng dụng tắt)
```

### Ví dụ

```java
@Service
public class CacheService {

    private Map<String, String> cache;

    @PostConstruct
    public void init() {
        // Chạy SAU khi dependency đã inject xong
        // Dùng để: load dữ liệu ban đầu, warm up cache, check connection...
        cache = new HashMap<>();
        log.info("CacheService initialized!");
    }

    @PreDestroy
    public void cleanup() {
        // Chạy TRƯỚC khi Bean bị hủy
        // Dùng để: đóng connection, giải phóng resource, flush data...
        cache.clear();
        log.info("CacheService destroyed!");
    }
}
```

### Constructor vs `@PostConstruct` — tại sao không dùng constructor luôn?

Trong constructor, dependency đã inject xong (nếu dùng Constructor Injection). Nhưng khi cần logic phức tạp phụ thuộc nhiều Bean khác, `@PostConstruct` an toàn hơn vì **tất cả dependency đã sẵn sàng 100%**:

```java
@Service
public class ReportService {

    private final DataSource dataSource;

    public ReportService(DataSource dataSource) {
        this.dataSource = dataSource;
        // dataSource đã có — nhưng constructor chỉ nên GÁN dependency
    }

    @PostConstruct
    public void init() {
        // Tất cả dependency sẵn sàng
        // An toàn để: load data, check connection, warm cache...
    }
}
```

> **Tư duy senior**: Constructor chỉ nên gán dependency. Logic khởi tạo phức tạp (load data, check connection, warm cache) nên để ở `@PostConstruct`.

---

## 6. Câu hỏi thực hành

### Q: `private` vs `private final` trong field

```java
// private — có thể bị thay đổi sau khi tạo
private String name;

public void someMethod() {
    this.name = "oops, changed!";  // ✅ Cho phép — có thể gây bug
}

// private final — gán 1 lần, không đổi được nữa
private final String name;

public void someMethod() {
    this.name = "oops";  // ❌ Compile error — compiler bảo vệ bạn
}
```

**Tại sao senior hay dùng `final`?**

1. **Ngăn bug do vô tình thay đổi** — Trong team 5-10 người, ai đó có thể viết `this.appName = "abc"` ở đâu đó mà không nhận ra. `final` khiến compiler chặn ngay.
2. **Thread-safety** — Spring Bean mặc định là Singleton, nhiều thread truy cập cùng lúc. `final` đảm bảo field không bị thread nào ghi đè.
3. **Rõ ý đồ thiết kế** — Thấy `final` là biết ngay: "field này gán 1 lần, không đổi". Không cần đọc hết class để kiểm tra.

**Khi nào cần `final`?**

| Trường hợp | Cần `final`? | Lý do |
|---|---|---|
| Bean field (Service, Controller) | **Nên** | Singleton, multi-thread, gán 1 lần |
| DTO trả về API | Tùy | Không bắt buộc, nhưng rõ ý đồ hơn |
| Entity (JPA) | **Không** | JPA cần setter để map dữ liệu từ database |

**Về DTO cụ thể**: `AppInfoResponse` không phải Bean, mỗi lần gọi API sẽ `new` object mới. `final` không ngăn việc tạo object mới với giá trị khác nhau — nó chỉ ngăn việc **thay đổi giá trị SAU khi tạo**. Với DTO, object tạo ra → trả về JSON → bị garbage collect, không ai cần sửa giữa chừng, nên `final` là "nice to have" chứ không bắt buộc.

> So sánh Node.js: `final` ↔ `const`, không có `final` ↔ `let`. Mặc định dùng `const`, chỉ dùng `let` khi cần thay đổi. **Quy tắc: mặc định luôn dùng `final`, chỉ bỏ khi thật sự cần thay đổi giá trị.**

### Q: DTO là gì? Tại sao tạo class riêng thay vì Map?

- **Type-safe** — compiler kiểm tra, không sợ typo key
- **Rõ ràng** — nhìn class biết ngay API trả về gì
- **Dễ mở rộng** — thêm validation, annotation sau này dễ dàng

`Map` nhanh cho prototype, nhưng dự án thật luôn dùng DTO.

---

## Tiến độ

- [x] Hiểu cấu trúc project: `controller` → `service` → `repository` → `entity`
- [x] Hiểu `application.properties` / `application.yml`
- [x] Viết REST API đơn giản với `@RestController`, `@GetMapping`, `@PostMapping`
- [x] Hiểu Bean lifecycle, Bean scope (Singleton vs Prototype)
- [x] Thực hành: AppInfoController + AppInfoService + DTO + config + lifecycle
